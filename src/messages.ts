module Messages {
    export interface errorMessages {

        ImportError: [
            "No such file or directory imported line \(n).",
            "File exists. But it is not a JS or CSSS file."
        ];

        TypeError: [
            "Type is invaild."
        ];
    }
}